// language=hbs
export const inputTmpl = `
    <input 
      id="{{ id }}" 
      name="{{ name }}" 
      type="{{type}}" 
      class="input {{className}}" 
      placeholder="{{placeholder}}"
    >
`
